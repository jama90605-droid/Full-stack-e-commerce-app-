# Copy to .env for local development, or set these in AWS Elastic Beanstalk
# Configuration → Software → Environment properties

PORT=3000

# Use * to allow requests from any origin (local, Vercel, etc.).
# Or set a comma-separated list to restrict access.
CORS_ORIGIN=*
