# Optional: override backend URL for local dev only (Vite proxy).
# Vercel uses vercel.json rewrites in production — no env var needed there.
VITE_BACKEND_URL=http://ecommerceproject-env.eba-kesmjpp4.eu-north-1.elasticbeanstalk.com
